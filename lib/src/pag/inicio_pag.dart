import 'package:flutter/material.dart';

class InicioPag extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('App Pagina Inicio*'),
      ),
      body: Center(
        child: Text('Pagina Inicio'),
      ),
    );
  }
}
